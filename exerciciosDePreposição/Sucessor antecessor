#include <stdio.h>
#include <stdlib.h>

int main() {
    int x, y, z;
    printf("Enter a number: ");
    scanf("%d", &x);
    y = x + 1;
    z = x - 1;
    printf("x = %d\n", x);
    printf("y = %d\n", y);
    printf("z = %d\n", z);
    return 0;
}
